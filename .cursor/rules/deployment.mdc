---
description: sales-data-agent 生产发版流程（Mac + OrcaTerm）
alwaysApply: true
---

# 发版与环境

- Mac 开发：`/Users/Eric/projects/sales-data-agent`
- 生产服务器：`/opt/sales-data-agent`（OrcaTerm），`http://43.136.25.181:3080`
- 不用 Trae 改服务器；看板 `.env` 与 `tools/alipay/` 无关

## 只改前端

1. Mac：`npm run pack:frontend` → `release/sales-dist-*.tgz`
2. 上传到服务器
3. OrcaTerm：`sudo tar xzf ... -C /opt/sales-data-agent` 然后 **`sudo docker compose up -d --build`**
4. 不要用 `docker compose restart`（不会更新镜像内 dist）

## 全量/后端

1. Mac：`npm run pack:deploy`
2. OrcaTerm：解压前 `cp .env /tmp/.env.backup`，解压后恢复 `.env`，再 `sudo docker compose up -d --build`

## 细节

完整步骤见 `docs/发版流程.md`。
